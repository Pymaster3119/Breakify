import React from 'react'
import { createRoot } from 'react-dom/client'
// Using AppClean because `App.jsx` contained stray markdown fences during edit; AppClean is the working copy
import App from './AppClean'
import './index.css'

createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
import React from 'react'
import { createRoot } from 'react-dom/client'
// Using AppClean because `App.jsx` contained stray markdown fences during edit; AppClean is the working copy
import App from './AppClean'
import './index.css'

createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
